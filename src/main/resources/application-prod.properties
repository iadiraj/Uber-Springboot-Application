spring.application.name=uber
# Database Configuration (using environment variables)
spring.datasource.url=${DB_HOST_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=create 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT (using a strong, randomly generated secret)
jwt.secret=${JWT_SECRET_KEY} 

# Management endpoints
management.endpoints.web.exposure.include=*
management.prometheus.metrics.export.enabled=true
management.endpoint.prometheus.enabled=true


# Email configuration
spring.mail.username=adityastudymisc2025@gmail.com
spring.mail.password=lknp eyon xdbg ifuf
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Commented Configurations
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:data.sql